# OS / editor cruft
.DS_Store
Thumbs.db
*.swp
*~

# Local extraction / handoff archives — never commit the raw design zips
*.zip
_extracted/
_staging/

# Node (only if a build step is ever added)
node_modules/
dist/
.cache/

# Env / secrets (Phase 2 /api/lead — never commit)
.env
.env.*
!.env.example
